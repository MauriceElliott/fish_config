if test (hostname) = KB-GH85FB4
    # Add taclaptop-specific configuration here
    set PATH $PATH /root/go/bin
    set PATH $PATH /home/mauricelliott/.local/bin
    set PATH $PATH ~/.local/bin
    alias hx nvim
end
